interface:
  display_name: "Game of Judgment"
  short_description: "Trace judgments into atomic micro-motives"
  default_prompt: "Use $game-of-judgment to trace my reaction into concise, separate micro-motives and save the ones I confirm."

policy:
  allow_implicit_invocation: true
